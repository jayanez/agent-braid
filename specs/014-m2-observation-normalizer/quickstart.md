# Quickstart

From a clean Python 3.12+ checkout, run the focused `tests/test_git_replay.py`
suite, `python3 -m scripts.validate_spec_kit`, and
`python3 scripts/validate_change.py --base develop --profile pr`.
Before implementation these are baseline checks, not evidence for this feature.
