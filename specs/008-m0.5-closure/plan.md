# Plan

Apply clause zero and Articles 13, 17–23 and 25. Refresh the milestone radar,
recreate the trends report without third-party template material, record the
history-level redistribution boundary and validate the current strategy and
market model. Freeze a candidate, reproduce it in a clean environment and obtain
three explicit founder decisions without changing repository visibility.

The public research-preview path defaults to a clean export while historical
report redistribution remains unresolved. Changing that path requires documented
rights or a separately approved history strategy; this feature never rewrites Git
history.
