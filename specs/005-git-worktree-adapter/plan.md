# Plan

Apply constitutional clause zero and Articles 1–7, 12–16, 19–21, 23 and 24.
Implement ADR 0008 and the Git request `0.1.0-alpha` contract without changing
AIM, certificates, assurance classes or the analysis report.

Use Git plumbing only for bounded observations. Bind resolved commits, normalized
path changes and snapshot hashes into generated AIM records. Reuse the existing
exact-resource analyzer and prefer rejection or unknown over unsafe inference.

Exercise commits and worktrees in temporary local repositories. Measurements
describe the corpus and environment; they do not establish product safety or
performance superiority.
