# Quickstart

From a clean Python 3.12+ checkout, run the focused counterexample tests,
`python3 -m scripts.validate_spec_kit`, and
`python3 scripts/validate_change.py --base develop --profile pr`.
Before implementation these are planned checks, not obtained evidence.
