# Tasks

- [x] T001 (REQ-001/SC-031): implement the dependency-free classifier and stable
  text/JSON/GitHub plan formats in `scripts/validate_change.py`; verify known paths,
  untracked paths, deterministic ordering and conservative unknown fallback with
  `ValidationProfileTests.test_classification_is_deterministic_and_unknown_paths_fail_closed`.
- [x] T002 (REQ-002/SC-032): implement baseline and affected-domain quick plans,
  direct subprocess execution and failure propagation. Verify that unrelated
  clean-room and Spec Kit integration commands are absent from ordinary plans.
- [x] T003 (REQ-003/SC-033): implement the complete PR command inventory and verify
  it retains repository, contracts, unit, science, governance/publication, Spec Kit
  render, Constitution replica and whitespace gates without evidence capture.
- [x] T004 (REQ-004/SC-034): encode normative, contract, scientific, supply-chain,
  process and Spec Kit specialist ownership; test that only generator/integration
  inputs activate the pinned solo/dual matrix and unknown paths fail closed.
- [x] T005 (REQ-005/SC-035): report milestone/release/publication/scientific
  clean-room procedures as deferred gates; test that no ordinary profile describes
  them as executed, approved or independently validated.
- [x] T006 (REQ-006/SC-036): split `.github/workflows/validate.yml` into parallel
  jobs with an unconditional complete candidate suite and conditional specialist
  integration job; add a structural workflow regression test.
- [x] T007: update `AGENTS.md`, `README.md`, `CONTRIBUTING.md`, Spec Kit guardrails
  and workflow documentation so local iteration, PR acceptance and evidence capture
  are distinct. Regenerate both official integrations from the shared override.
- [x] T008: add proposed ADR 0011 and complete feature documentation and assurance
  traceability without changing constitutional or public contract bytes.
- [x] T009: run targeted negative tests, the quick and PR profiles, the real pinned
  Codex/Claude matrix, all repository validators and `git diff --check`; capture
  bounded actual evidence and leave human review plus ADR acceptance pending.
