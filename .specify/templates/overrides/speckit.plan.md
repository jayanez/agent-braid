---
description: Plan an Agent Braid specification with constitutional gates and bounded scientific validation.
scripts:
  py: scripts/python/setup_plan.py --json
---

Consider the user's input: $ARGUMENTS

Run `{SCRIPT}` and read the resulting feature spec and plan paths. Use the
overridden plan template. Before design, map applicable constitutional obligations
and check the assurance record. Resolve unknowns through documented research;
leave genuinely unresolved scientific questions explicit. Do not dispatch agents.
Write `research.md` with decisions, evidence, alternatives and limits. Describe
data models and interfaces only where in scope, without changing public contracts
implicitly. Write `quickstart.md` with repeatable validation commands and expected
outcomes. Recheck obligations after design. Report blockers instead of justifying
constitutional violations. Planning is not permission to implement or deliver.
